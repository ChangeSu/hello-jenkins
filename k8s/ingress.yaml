apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-jenkins-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: hello.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-jenkins-service
                port:
                  number: 80